#!/usr/bin/env bash
set -euo pipefail

self_dir=$(CDPATH= cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)
stats_script="$self_dir/pi-secretary-stats.py"
if [[ ! -r "$stats_script" ]]; then stats_script="$self_dir/../scripts/pi-secretary-stats.py"; fi
exec python3 "$stats_script" "$@"
