// The host-side scope is enforced by scripts/pi_control/scoped_read.py.  This
// file is intentionally tiny so the extension cannot grow an alternate path.
export const SCOPED_READ_TOOL_NAMES = ["read", "ls", "grep", "git_read"];
