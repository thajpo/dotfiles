--- a/src/extension/fanout-child.ts
+++ b/src/extension/fanout-child.ts
@@ -28,9 +28,10 @@
 	return p.startsWith("~/") ? path.join(os.homedir(), p.slice(2)) : p;
 }
 
-const WORKER_READ_ONLY_TOOLS = ["read", "grep", "find", "ls", "contact_supervisor", "host_command"];
+const WORKER_READ_ONLY_TOOLS = ["read", "grep", "find", "ls", "web_search", "fetch_content", "get_search_content", "source_check", "contact_supervisor", "intercom", "host_command"];
 const WORKER_READ_ONLY_ACCEPTANCE = { level: "none" as const, reason: "worker read-only fanout" };
 const HOST_COMMAND_EXTENSION = path.join(getAgentDir(), "extensions", "host-command", "index.ts");
+const WEB_ACCESS_EXTENSION = path.join(getAgentDir(), "npm", "node_modules", "pi-web-access", "index.ts");
 
 function workerReadOnlyFanout(): boolean {
 	return process.env[SUBAGENT_CHILD_AGENT_ENV]?.trim() === "worker";
@@ -47,7 +48,7 @@
 		tools: WORKER_READ_ONLY_TOOLS.filter((tool) => tool !== "contact_supervisor" || agent.tools?.includes(tool)),
 		mcpDirectTools: [],
 		extensions: [],
-		subagentOnlyExtensions: [HOST_COMMAND_EXTENSION],
+		subagentOnlyExtensions: [HOST_COMMAND_EXTENSION, WEB_ACCESS_EXTENSION],
 		inheritProjectContext: false,
 		inheritSkills: false,
 		output: undefined,
